{
  "name": "my-caribbean-journey",
  "version": "1.0.0",
  "description": "My Caribbean Journey — books, island guides, and blog",
  "scripts": {
    "build": "node scripts/sync-tags.js && eleventy",
    "start": "node scripts/sync-tags.js && eleventy --serve"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.1.5"
  }
}
